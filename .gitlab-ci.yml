workflow:
  rules:
    - if: $CI_COMMIT_TAG =~ /^v/
    - if: $CI_COMMIT_BRANCH == "main"
    - when: never

stages:
  - build
  - deploy
  - release

variables:
  CARGO_HOME: "${CI_PROJECT_DIR}/.cargo"
  npm_config_cache: "${CI_PROJECT_DIR}/.npm"
  # Pull in the rustafits submodule on every job's checkout. Without this
  # the runner logs "Skipping Git submodules setup" and `cargo metadata`
  # fails with "failed to read rustafits/Cargo.toml" because the workspace
  # member isn't on disk. `recursive` covers any nested submodule rustafits
  # itself might add later.
  GIT_SUBMODULE_STRATEGY: recursive
  GIT_SUBMODULE_DEPTH: 1

# --- Windows Build ---
build:windows:
  stage: build
  tags:
    - windows
  only:
    - tags
  needs:
    - job: build:linux
      artifacts: false
  cache:
    key: "main-windows"
    policy: pull
    paths:
      - .cargo/registry/
      - .cargo/git/
      - target/
      - .npm/
  script:
    # --- Ensure native build deps for turbojpeg-sys (libjpeg-turbo) ---
    # cmake and nasm are needed because turbojpeg-sys builds libjpeg-turbo
    # from source on every fresh build. Idempotent: install only what's
    # missing, then refresh PATH so this job sees the new binaries.
    - |
      $missing = @('cmake','nasm') | Where-Object { -not (Get-Command $_ -ErrorAction SilentlyContinue) }
      if ($missing) {
        Write-Host "Installing missing build tools: $($missing -join ', ')"
        choco install -y --no-progress $missing
        $env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
      }
    - if (Test-Path "target\release\bundle") { Remove-Item -Recurse -Force "target\release\bundle" }
    - npm install
    - npm run tauri build
  artifacts:
    name: "athenaeum-windows-${CI_COMMIT_SHORT_SHA}"
    paths:
      - target/release/bundle/msi/*.msi
      - target/release/bundle/nsis/*.exe
    expire_in: 30 days

# --- Linux Build ---
build:linux:
  stage: build
  tags:
    - linux
  only:
    - tags
  before_script:
    - source "$HOME/.cargo/env"
    # cmake + nasm are required by rustafits → turbojpeg-sys → libjpeg-turbo (built from source).
    # They must be pre-installed on the runner host because the gitlab-runner user
    # cannot sudo non-interactively. One-time setup on the runner host:
    #   sudo apt-get install -y cmake nasm
    - |
      missing=()
      command -v cmake >/dev/null || missing+=(cmake)
      command -v nasm  >/dev/null || missing+=(nasm)
      if [ ${#missing[@]} -gt 0 ]; then
        echo "ERROR: missing build deps on runner: ${missing[*]}"
        echo "Run on the runner host: sudo apt-get install -y cmake nasm"
        exit 1
      fi
  cache:
    key: "main-linux"
    policy: pull
    paths:
      - .cargo/registry/
      - .cargo/git/
      - target/
      - .npm/
  script:
    - export RUSTAFITS_PORTABLE=1
    - rm -rf target/release/bundle/
    - npm install
    - npm run tauri build
  artifacts:
    name: "athenaeum-linux-${CI_COMMIT_SHORT_SHA}"
    paths:
      - target/release/bundle/appimage/*.AppImage
      - target/release/bundle/deb/*.deb
    expire_in: 30 days

# --- macOS Build (Apple Silicon + Intel) ---
build:macos:
  stage: build
  tags:
    - macos
  only:
    - tags
  cache:
    key: "main-macos"
    policy: pull
    paths:
      - .cargo/registry/
      - .cargo/git/
      - target/
      - .npm/
  script:
    # --- Ensure native build deps for turbojpeg-sys (libjpeg-turbo) ---
    # cmake and nasm are needed because turbojpeg-sys builds libjpeg-turbo
    # from source on every fresh build.
    - for tool in cmake nasm; do command -v $tool >/dev/null || brew install $tool; done
    - export PATH="$HOME/.cargo/bin:$PATH"
    - which cargo && rustc --print sysroot
    - rustup target add x86_64-apple-darwin
    - rm -rf target/aarch64-apple-darwin/release/bundle/
    - rm -rf target/x86_64-apple-darwin/release/bundle/
    - npm install
    - npm run tauri build -- --target aarch64-apple-darwin
    - npm run tauri build -- --target x86_64-apple-darwin
  artifacts:
    name: "athenaeum-macos-${CI_COMMIT_SHORT_SHA}"
    paths:
      - target/aarch64-apple-darwin/release/bundle/dmg/*.dmg
      - target/x86_64-apple-darwin/release/bundle/dmg/*.dmg
    expire_in: 30 days

# --- Cache Warmers (update cache periodically on main) ---
update-cache:windows:
  stage: build
  tags:
    - windows
  only:
    - main
  when: manual
  cache:
    key: "main-windows"
    policy: push
    paths:
      - .cargo/registry/
      - .cargo/git/
      - target/
      - .npm/
  script:
    # --- Ensure native build deps for turbojpeg-sys (libjpeg-turbo) ---
    - |
      $missing = @('cmake','nasm') | Where-Object { -not (Get-Command $_ -ErrorAction SilentlyContinue) }
      if ($missing) {
        choco install -y --no-progress $missing
        $env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
      }
    - npm install
    - npm run tauri build

update-cache:linux:
  stage: build
  tags:
    - linux
  only:
    - main
  when: manual
  before_script:
    - source "$HOME/.cargo/env"
    # cmake + nasm must be pre-installed on the runner host (see build:linux for details).
    - |
      missing=()
      command -v cmake >/dev/null || missing+=(cmake)
      command -v nasm  >/dev/null || missing+=(nasm)
      if [ ${#missing[@]} -gt 0 ]; then
        echo "ERROR: missing build deps on runner: ${missing[*]}"
        echo "Run on the runner host: sudo apt-get install -y cmake nasm"
        exit 1
      fi
  cache:
    key: "main-linux"
    policy: push
    paths:
      - .cargo/registry/
      - .cargo/git/
      - target/
      - .npm/
  script:
    - export RUSTAFITS_PORTABLE=1
    - npm install
    - npm run tauri build

update-cache:macos:
  stage: build
  tags:
    - macos
  only:
    - main
  when: manual
  cache:
    key: "main-macos"
    policy: push
    paths:
      - .cargo/registry/
      - .cargo/git/
      - target/
      - .npm/
  script:
    # --- Ensure native build deps for turbojpeg-sys (libjpeg-turbo) ---
    - for tool in cmake nasm; do command -v $tool >/dev/null || brew install $tool; done
    - export PATH="$HOME/.cargo/bin:$PATH"
    - rustup target add x86_64-apple-darwin
    - npm install
    - npm run tauri build -- --target aarch64-apple-darwin
    - npm run tauri build -- --target x86_64-apple-darwin

# --- Deploy to Web Server (tags only) ---
deploy:
  stage: deploy
  tags:
    - linux
  only:
    - tags
  variables:
    DEPLOY_PORT: "40022"
    BUILDS_PATH: "/var/www/artfrom.space/builds"
    SSH_CMD: "ssh -p ${DEPLOY_PORT}"
    SCP_CMD: "scp -P ${DEPLOY_PORT}"
  needs:
    - job: build:linux
      artifacts: true
    - job: build:windows
      artifacts: true
    - job: build:macos
      artifacts: true
  before_script:
    - eval $(ssh-agent -s)
    - chmod 400 "$DEPLOY_SSH_KEY"
    - ssh-add "$DEPLOY_SSH_KEY"
    - mkdir -p ~/.ssh && chmod 700 ~/.ssh
    - ssh-keyscan -p ${DEPLOY_PORT} -H "$DEPLOY_HOST" >> ~/.ssh/known_hosts 2>/dev/null
  script:
    # Rename artifacts to include tag in filename
    - mv target/release/bundle/appimage/*.AppImage "target/release/bundle/appimage/athenaeum_${CI_COMMIT_TAG}_amd64.AppImage"
    - mv target/release/bundle/deb/*.deb "target/release/bundle/deb/athenaeum_${CI_COMMIT_TAG}_amd64.deb"
    - mv target/release/bundle/msi/*.msi "target/release/bundle/msi/Athenaeum_${CI_COMMIT_TAG}_x64_en-US.msi"
    - mv target/release/bundle/nsis/*.exe "target/release/bundle/nsis/Athenaeum_${CI_COMMIT_TAG}_x64-setup.exe"
    - mv target/aarch64-apple-darwin/release/bundle/dmg/*.dmg "target/aarch64-apple-darwin/release/bundle/dmg/Athenaeum_${CI_COMMIT_TAG}_aarch64.dmg"
    - mv target/x86_64-apple-darwin/release/bundle/dmg/*.dmg "target/x86_64-apple-darwin/release/bundle/dmg/Athenaeum_${CI_COMMIT_TAG}_x64.dmg"
    # Create directories on remote
    - ${SSH_CMD} ${DEPLOY_USER}@${DEPLOY_HOST} "mkdir -p ${BUILDS_PATH}/${CI_COMMIT_TAG}/linux ${BUILDS_PATH}/${CI_COMMIT_TAG}/windows ${BUILDS_PATH}/${CI_COMMIT_TAG}/macos"
    # Upload Linux artifacts
    - ${SCP_CMD} target/release/bundle/appimage/*.AppImage target/release/bundle/deb/*.deb ${DEPLOY_USER}@${DEPLOY_HOST}:${BUILDS_PATH}/${CI_COMMIT_TAG}/linux/
    # Upload Windows artifacts
    - ${SCP_CMD} target/release/bundle/msi/*.msi target/release/bundle/nsis/*.exe ${DEPLOY_USER}@${DEPLOY_HOST}:${BUILDS_PATH}/${CI_COMMIT_TAG}/windows/
    # Upload macOS artifacts
    - ${SCP_CMD} target/aarch64-apple-darwin/release/bundle/dmg/*.dmg target/x86_64-apple-darwin/release/bundle/dmg/*.dmg ${DEPLOY_USER}@${DEPLOY_HOST}:${BUILDS_PATH}/${CI_COMMIT_TAG}/macos/
    # Update symlink: beta tags → builds/beta, stable tags → builds/latest
    - |
      if echo "${CI_COMMIT_TAG}" | grep -q '\-beta'; then
        ${SSH_CMD} ${DEPLOY_USER}@${DEPLOY_HOST} "ln -sfn ${BUILDS_PATH}/${CI_COMMIT_TAG} ${BUILDS_PATH}/beta"
      else
        ${SSH_CMD} ${DEPLOY_USER}@${DEPLOY_HOST} "ln -sfn ${BUILDS_PATH}/${CI_COMMIT_TAG} ${BUILDS_PATH}/latest"
      fi
  after_script:
    - ssh-agent -k 2>/dev/null || true

# --- Release (only on tags) ---
release:
  stage: release
  tags:
    - linux
  only:
    - tags
  needs:
    - job: deploy
      artifacts: false
  script:
    - |
      curl --fail --silent --show-error \
        --request POST \
        --header "JOB-TOKEN: ${CI_JOB_TOKEN}" \
        --header "Content-Type: application/json" \
        --data "{
          \"tag_name\": \"${CI_COMMIT_TAG}\",
          \"name\": \"Athenaeum ${CI_COMMIT_TAG}\",
          \"description\": $(cat RELEASE_NOTES.md 2>/dev/null | python3 -c 'import sys,json; print(json.dumps(sys.stdin.read()))' 2>/dev/null || echo '\"Release '${CI_COMMIT_TAG}'\"'),
          \"assets\": {
            \"links\": [
              {\"name\": \"Windows Installer (MSI)\", \"url\": \"https://artfrom.space/builds/${CI_COMMIT_TAG}/windows/Athenaeum_${CI_COMMIT_TAG}_x64_en-US.msi\"},
              {\"name\": \"Windows Installer (EXE)\", \"url\": \"https://artfrom.space/builds/${CI_COMMIT_TAG}/windows/Athenaeum_${CI_COMMIT_TAG}_x64-setup.exe\"},
              {\"name\": \"Linux AppImage\", \"url\": \"https://artfrom.space/builds/${CI_COMMIT_TAG}/linux/athenaeum_${CI_COMMIT_TAG}_amd64.AppImage\"},
              {\"name\": \"Linux Debian Package\", \"url\": \"https://artfrom.space/builds/${CI_COMMIT_TAG}/linux/athenaeum_${CI_COMMIT_TAG}_amd64.deb\"},
              {\"name\": \"macOS Disk Image (Apple Silicon)\", \"url\": \"https://artfrom.space/builds/${CI_COMMIT_TAG}/macos/Athenaeum_${CI_COMMIT_TAG}_aarch64.dmg\"},
              {\"name\": \"macOS Disk Image (Intel)\", \"url\": \"https://artfrom.space/builds/${CI_COMMIT_TAG}/macos/Athenaeum_${CI_COMMIT_TAG}_x64.dmg\"}
            ]
          }
        }" \
        "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/releases"

publish_version:
  stage: release
  tags:
    - linux
  only:
    - tags
  needs:
    - job: deploy
      artifacts: false
  variables:
    DEPLOY_PORT: "40022"
    SSH_CMD: "ssh -p ${DEPLOY_PORT}"
    SCP_CMD: "scp -P ${DEPLOY_PORT}"
  before_script:
    - eval $(ssh-agent -s)
    - chmod 400 "$DEPLOY_SSH_KEY"
    - ssh-add "$DEPLOY_SSH_KEY"
    - mkdir -p ~/.ssh && chmod 700 ~/.ssh
    - ssh-keyscan -p ${DEPLOY_PORT} -H "$DEPLOY_HOST" >> ~/.ssh/known_hosts 2>/dev/null
  script:
    - VERSION=${CI_COMMIT_TAG#v}
    - |
      if echo "${CI_COMMIT_TAG}" | grep -q '\-beta'; then
        TARGET="version-beta.json"
      else
        TARGET="version.json"
      fi
    - printf '{"version":"%s","download_url":"https://artfrom.space/releases/download"}' "$VERSION" > "$TARGET"
    - ${SCP_CMD} "$TARGET" ${DEPLOY_USER}@${DEPLOY_HOST}:/var/www/artfrom.space/"$TARGET"
  after_script:
    - ssh-agent -k 2>/dev/null || true

# --- Chat Notifications (only on tags) ---
# After the GitLab Release exists, post the contents of RELEASE_NOTES.md to
# Discord and Telegram. Both are siblings under `needs: [release]` so a failure
# in one platform does not prevent the other from posting.
#
# Required CI/CD variables (Settings -> CI/CD -> Variables; Masked, Protected):
#   - DISCORD_WEBHOOK_URL  (full webhook URL)
#   - TELEGRAM_BOT_TOKEN   (BotFather token, e.g. 1234567:ABC-DEF...)
#   - TELEGRAM_CHAT_ID     (numeric chat id, or @channelusername)
# Each job skips cleanly with a log message if its required variables are unset,
# so the first pipeline after merging this change does not go red while you
# add the secrets.
#
# Spec: docs/superpowers/specs/2026-05-11-release-notes-chat-notifications-design.md

notify:discord:
  stage: release
  tags:
    - linux
  only:
    - tags
  needs:
    - job: release
      artifacts: false
  allow_failure: true
  script:
    - .gitlab/ci/scripts/notify_discord.sh

notify:telegram:
  stage: release
  tags:
    - linux
  only:
    - tags
  needs:
    - job: release
      artifacts: false
  allow_failure: true
  script:
    - .gitlab/ci/scripts/notify_telegram.sh

# --- Docker Hub Publish (only on tags) ---
# Build the docker/Dockerfile image multi-arch (linux/amd64 + linux/arm64)
# and push to Docker Hub. Sibling under `needs: [release]` so a Docker Hub
# outage cannot fail the GitLab Release itself.
#
# Tag derivation (mirrors the retired .github/workflows/docker.yml):
#   v0.2.0           -> :0.2.0 + :latest
#   v0.2.1-beta.1    -> :0.2.1-beta.1 + :beta
# Anything else with a non-"-beta" suffix (-rc, -alpha) routes to :latest;
# tag those carefully or widen the grep below.
#
# Required CI/CD variables (Settings -> CI/CD -> Variables; Masked, Protected):
#   - DOCKERHUB_USERNAME  (Docker Hub username, e.g. vsharifov)
#   - DOCKERHUB_TOKEN     (Docker Hub access token; Read, Write, Delete)
# Skips cleanly with a log message if either is unset.
#
# Runner host prereqs (one-time, requires sudo on the linux runner):
#   sudo apt-get install -y docker.io docker-buildx qemu-user-static
#   sudo usermod -aG docker gitlab-runner && sudo systemctl restart gitlab-runner
#   sudo -u gitlab-runner docker run --rm --privileged tonistiigi/binfmt --install arm64

publish:dockerhub:
  stage: release
  tags:
    - linux
  only:
    - tags
  needs:
    - job: release
      artifacts: false
  allow_failure: true
  variables:
    IMAGE: "docker.io/vsharifov/athenaeum"
  before_script:
    # buildx must be pre-installed on the runner host (see header above).
    - |
      missing=()
      command -v docker >/dev/null || missing+=(docker)
      docker buildx version >/dev/null 2>&1 || missing+=(docker-buildx)
      if [ ${#missing[@]} -gt 0 ]; then
        echo "ERROR: missing build deps on runner: ${missing[*]}"
        echo "Run on the runner host:"
        echo "  sudo apt-get install -y docker.io docker-buildx qemu-user-static"
        echo "  sudo usermod -aG docker gitlab-runner && sudo systemctl restart gitlab-runner"
        exit 1
      fi
  script:
    - |
      if [ -z "${DOCKERHUB_USERNAME:-}" ] || [ -z "${DOCKERHUB_TOKEN:-}" ]; then
        echo "DOCKERHUB_USERNAME / DOCKERHUB_TOKEN not set; skipping Docker Hub publish."
        exit 0
      fi
    - VERSION="${CI_COMMIT_TAG#v}"
    - |
      if echo "${CI_COMMIT_TAG}" | grep -q '\-beta'; then
        CHANNEL_TAG="beta"
      else
        CHANNEL_TAG="latest"
      fi
    - echo "Publishing ${IMAGE}:${VERSION} and ${IMAGE}:${CHANNEL_TAG}"
    - echo "${DOCKERHUB_TOKEN}" | docker login -u "${DOCKERHUB_USERNAME}" --password-stdin docker.io
    # Idempotent buildx builder bootstrap.
    - docker buildx inspect athenaeum-builder >/dev/null 2>&1 || docker buildx create --name athenaeum-builder --use
    - docker buildx use athenaeum-builder
    - |
      docker buildx build \
        --platform linux/amd64,linux/arm64 \
        --file docker/Dockerfile \
        --tag "${IMAGE}:${VERSION}" \
        --tag "${IMAGE}:${CHANNEL_TAG}" \
        --label "org.opencontainers.image.title=Athenaeum" \
        --label "org.opencontainers.image.description=Astrophotography image management and FITS/XISF catalog" \
        --label "org.opencontainers.image.version=${VERSION}" \
        --label "org.opencontainers.image.source=${CI_PROJECT_URL}" \
        --push \
        .
  after_script:
    - docker logout docker.io 2>/dev/null || true
