//! Calibration Library: fixed v1 master-file naming (`paths`) and
//! consolidated master FITS header construction (`headers`) for Phase 2
//! master calibration. `register.rs` (Task 11) builds runtime plumbing on
//! top of these — not declared here yet.

pub mod headers;
pub mod paths;
