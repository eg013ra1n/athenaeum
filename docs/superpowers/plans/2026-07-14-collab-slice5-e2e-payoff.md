# Collab Slice 5 — E2E + Payoff (Project WBPP Export, Notifications Polish) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** The processor's payoff and the proof of the whole Stage II: a project-scoped WBPP export ("export combined dataset, calibration off" — spec §7), the three-instance E2E scenario (§11: contributor → gated publish → push-seed → moderation → swarm delivery → export), and the §9 notifications polish (replicated / downloaded / awaiting-approval / joined-project).

**Architecture:** The export reuses the existing UNGATED WBPP machinery wholesale — `ExportFrame`/`ExportGroup`/`make_group_key`/`organize_files_wbpp` stay untouched; the only new piece is a collector that builds **one `ExportData` per publisher** from `project_contributions` (landed paths + snake_case `frame_meta` snapshots) plus one for the processor's own calibrated outputs of the project's linked sets; the runner invokes the organizer once per publisher under `<out>/<title>/` (Д2). Notifications ride two existing seams: `SyncFinishedEvent.projectId` (already on the wire since slice 4) branches `notifyFinished` project-aware, and the poll diff gains an `awaitingApproval` kind. The E2E lives IN-CRATE (`#[cfg(test)]`) because its building blocks (`store_token_for_test`, `set_started_for_test`, seed helpers) are `cfg(test)`-only — the audited explorer verdict.

**Tech Stack:** Rust (existing export module — ungated; wiremock + LoopbackTransport), React 18 + TS.

## Global Constraints

- **Repo/branch:** athenaeum, continue on `0.5.0` (slice-4 tip `4ae96dfa`). No version bumps.
- **Two backends in sync:** the one new command ships Tauri + Axum in the same commit; wrappers thin; `#[tracing::instrument(skip_all, err)]` / `err(Debug)`.
- **Feature gating:** `export/` is UNGATED (verified: `lib.rs:24` no cfg) and the calibration-off path (`ExportMode::RawWithCalibrationSets`) touches no gated code — the project collector + export command live UNGATED (`api/collab_exchange.rs`). `cargo build -p perseus --no-default-features` stays green. The E2E may call render-gated `publish_collab_frames` — test targets build with default features.
- **Never swallow errors**; notifications only via `notify()`; design tokens; `api.listen` cancelled-flag; models.ts regenerated never hand-edited.
- **Export reuse contract (verified against code):** `ExportFrame` fields map from contributions as `file_path = landed_path`, `filename` = basename of `landed_path`; `filter`/`bayerpat`/`instrume`/`exptime` parse from `frame_meta` (a snake_case `models::Frame` snapshot — NO serde rename on `Frame`); grouping is `(filter, CameraType::from_bayerpat)` ONLY (no exposure/night); `organize_files_wbpp(output_dir, data, use_symlinks, config, emitter, frame_set_id, cancel_flag)` skips existing dests (never overwrites) and warns-not-fails on missing sources; each organizer call roots itself at `<output_dir>/<sanitize_display_folder_name(data.frame_set_name)>` — under Д2 that means output_dir = `<out>/<sanitized title>` and frame_set_name = the publisher display, yielding the per-publisher folder.
- **Events:** project export reuses `"export-progress"`/`"export-complete"` (`ExportProgressEvent`/`ExportCompleteEvent`) with the sentinel `frame_set_id = -1` (Д3).
- **Tests:** slice gates: `cargo build --workspace`, `cargo test -p athenaeum-core` (FULL — incl. ts_contract), `cargo test -p perseus`, `cargo build -p perseus --no-default-features`, `npx tsc --noEmit`.
- **Commit identity:** `eg013ra1n <vilen.sharifov@gmail.com>`, never a Claude author/co-author line.

## Design decisions (Д1–Д5, for owner review)

- **Д1 — "Combined dataset" = received contributions ∪ own calibrated outputs.** Received: non-superseded `project_contributions` rows. Own: every LIGHT frame of the project's linked sets that has a `light_calibrations` row whose `output_path` exists on disk — NOT gate-filtered (the gate is render-gated and quality-gating is the contributor's publish-time choice; locally the processor stacks what it has). Own frames' metadata comes from the `frames` catalog row.
- **Д2 — Per-publisher subtrees, original filenames (owner decision 2026-07-14):** the tree is `<out>/<title>/<publisher display>/camera_<instrume>/lights/<original basename>` — one subtree per publisher (folder = publisher display name), files keep the name they had on the publisher's side (`basename(landed_path)` — landing preserves the publisher-side filename; NO prefixing). Own calibrated frames get the symmetric treatment under the local member's display-name folder. GATING (verified): `own_display_name` (`api/collab.rs:1086`) and `member_node_ids` (`:1073`) are private fns inside the RENDER-GATED `api::collab` module — the ungated runner cannot call them in a headless build. Resolution: MOVE both helpers to ungated `crate::collab::snapshot` (where `SnapshotMember` already lives, `collab/snapshot.rs:22`, pub) as `pub(crate)`, update the `api/collab.rs` call sites to import them (render-gated code may import from ungated, never the reverse). Inputs are all ungated: `own_node` via the `collab.rs:1210-1213` idiom (`sync_paths(ctx)` → `device_key_path` → `DeviceKey::load_or_create(..).node_id()`), members via `get_project().members_json` parsed as `Vec<SnapshotMember>`. Unresolvable ⇒ folder `"own"` + `warn!`. Achieved WITHOUT touching the organizer: the runner calls `organize_files_wbpp` once per publisher with `output_dir = <out>/<sanitized title>` and `ExportData.frame_set_name = publisher display` (the organizer's own `sanitize_display_folder_name` makes the folder — no double-sanitizing in the collector). Residual risk (accepted, documented): two publishers sharing a display name merge into one folder; same-basename collisions there hit the organizer's skip-existing + warn semantics.
- **Д3 — Project export rides the existing export surface with `frame_set_id = -1`** (sentinel, documented): same events, same `active_exports` cancel registry, same frontend `ExportProgress` context + `kind:'export'` completion notification — all for free. Limitation (accepted): one project export at a time; a concurrent frame-set export with id −1 is impossible (real ids are positive).
- **Д4 — Notifications polish:** (a) `downloadComplete` is REMOVED from the documented `PackageStateChange` kinds (it was never produced); the receive-side notification comes instantly from `SyncFinishedEvent.projectId` instead. (b) `notifyFinished` branches project-aware: sent+confirmed+projectId → "Contribution replicated — safe to go offline" (`kind:'project'`, link `/projects/<id>`, dedupe `collab-sent-<packageId>`); received+projectId → "Project package downloaded — N frames" (`kind:'project'`, dedupe `collab-recv-<packageId>`), and the sync-incoming-unconfigured NUDGE IS SKIPPED for project events (collab lands in the collaboration root, the nudge would mislead). (c) New poll-diff kind `awaitingApproval`: an unknown row arriving `pending && !own` (only coordinators ever receive foreign pending rows — hub visibility enforces the audience; VERIFIED: the unknown-row arm at `collab_exchange.rs:548-556` currently emits `newPackage` only for `published && !own`, so the pending case slots in beside it) → frontend "Contribution awaiting your approval" (`link:/projects/<id>`, dedupe `pkg-approval-<packageId>`). (d) **Joined-project notification (§9 "join request approved" — spec-coverage gap, currently NOTHING signals a granted membership in-app):** pure frontend diff in `useProjects` — no Rust change; when a refresh returns a `ProjectCard` whose `projectId` was not in the previously-known set, notify. Coordinator cards are SKIPPED (the creator watching their own new project appear is not a join approval). Accepted noise: first sign-in on a fresh install notifies once per pre-existing project — the localStorage-persisted dedupe caps it at once ever per project.
- **Д5 — E2E is in-crate `#[cfg(test)]`, multi-thread tokio, `#[cfg(unix)]`** — mirrors `download_happy_path_over_loopback` (the only existing multi-endpoint collab test and the closest template), because the seams it needs (`wire_hub`, `store_token_for_test`, `set_started_for_test`, seed helpers) do not exist for out-of-crate integration targets.

## Task overview (4)

1. Project export collector + export runner (core, ungated) — `collect_project_export_data` + `export_project_for_wbpp`.
2. Export command wiring (both backends) + ProjectDetail export UI.
3. Notifications polish — `awaitingApproval` diff (rust) + project-aware `notifyFinished` + useProjects mapping + joined-project diff (frontend).
4. Three-instance E2E (§11) — contributor→coordinator(approval)→processor(swarm download)→WBPP export, one wiremock hub, loopback network.

---

### Task 1: Project export collector + runner (ungated core)

**Files:**
- Create: `crates/athenaeum-core/src/export/project_collector.rs`; Modify: `crates/athenaeum-core/src/export/mod.rs` (declare + re-export)
- Modify: `crates/athenaeum-core/src/api/collab_exchange.rs` (the runner `export_project_for_wbpp`)
- Modify: `crates/athenaeum-core/src/collab/snapshot.rs` (receive the moved `own_display_name`/`member_node_ids` helpers — Д2 gating) + `crates/athenaeum-core/src/api/collab.rs` (call sites import the moved helpers)

**Interfaces:**
- Consumes (all verified file:line): `db::collab_exchange::contributions_for_project(conn, project_id) -> Result<Vec<ContributionRow>>` (`collab_exchange.rs:380`; row (`:277-296`): `landed_path`, `frame_meta: String`, `publisher_display`, `superseded: bool`, `frame_uuid`, `rel_path`, `byte_size`, `xxh3`, `analysis: Option<String>`); `db::collab::linked_set_ids(conn, project_id) -> Result<Vec<i64>>` (`collab.rs:184`) + `get_project` (`collab.rs:140` → `CollabProjectRow { title, target_name, members_json, .. }`); the LIGHT-frames union SQL — `union_light_frames` is PRIVATE in render-gated `api/collab.rs:234`, so REPRODUCE its join (`:243-250`): `session_members sm → sessions s → imaging_nights ino → frames f → files fi`, `WHERE ino.frames_set_id IN (…) AND f.imagetyp = 'Light'`, `SELECT DISTINCT sm.frame_id, fi.filename`; `db::light_calibrations::get_light_calibration_for_frame(conn, frame_id) -> Result<Option<LightCalRow>>` (`light_calibrations.rs:172`; `LightCalRow.output_path: String`); `export::models::{ExportFrame, ExportGroup, ExportData, CameraType, sanitize_folder_name, sanitize_display_folder_name}` (AUDIT: `export/mod.rs` does NOT re-export models — use `crate::export::models::` paths or add `pub use models::{...};` in Task-1 step 2 and keep it minimal; `make_group_key`/`make_display_name` are ASSOCIATED fns `ExportGroup::make_group_key(filter, camera_type)`, models.rs:165-174, not free fns); `export::file_organizer::organize_files_wbpp` (file_organizer.rs:112); `WbppExportConfig` (settings key `"export.wbpp_config"`); `crate::api::db` conn idiom; `active_exports` registry — CONFIRMED a core field: `ServiceContext.active_exports: Arc<Mutex<HashMap<i64, ExportHandle>>>` (`services/mod.rs:84`; `ExportHandle { cancel_flag: Arc<AtomicBool> }` `:31-33`; register/deregister idiom `commands/export.rs:477-483`/`:596-599`).
- Produces (BINDING for Tasks 2/4):

```rust
/// Д1/Д2: one ExportData per publisher — received (non-superseded) contributions
/// grouped by publisher_display, plus one dataset for the processor's own
/// calibrated outputs (key = own_display). Pure catalog/db read — no hub I/O, ungated.
pub struct ProjectExportData {
    pub title: String,                         // project title → root folder
    pub publishers: Vec<(String, ExportData)>, // (publisher display → folder, dataset)
}

pub fn collect_project_export_data(
    conn: &rusqlite::Connection,
    project_id: &str,
    own_display: &str, // resolved by the runner via the MOVED own_display_name helper (Д2)
) -> anyhow::Result<ProjectExportData>;

/// The runner: collect → for each publisher, organize under
/// <output_dir>/<sanitized project title>/ with frame_set_name = publisher display
/// (Д2 — one organizer call per publisher). Rides the standard export events with
/// the Д3 sentinel frame_set_id = -1.
pub async fn export_project_for_wbpp(
    ctx: &crate::services::ServiceContext,
    project_id: &str,
    output_dir: &str,
    use_symlinks: bool,
    emitter: Option<std::sync::Arc<dyn crate::events::ProgressEmitter>>,
) -> Result<crate::export::models::ExportResult, crate::api::ApiError>;
```

Collector rules (each a required behavior):
1. Project row must exist (`db::collab::get_project`) — missing ⇒ error "project not in the local cache". `ProjectExportData.title` = the project **title**; each per-publisher `ExportData.frame_set_name` = that **publisher's display name** (the organizer sanitizes it into the folder — Д2); `frame_set_id = -1` on every dataset.
2. Received: `contributions_for_project`, skip `superseded` rows; **partition rows by `publisher_display`** — one `ExportData` per publisher; per row parse `frame_meta` JSON (snake_case `Frame` snapshot): `filter`, `bayerpat`, `instrume`, `exptime` (+ `ccd_temp`/`gain`/`offset`/`binning`/`date_obs`/`focallen`/`xpixsz` where `ExportFrame` wants them — absent ⇒ None, never invented; malformed JSON ⇒ skip the row with `tracing::warn!`, count into a warnings list). `ExportFrame.file_path = landed_path`, `filename = basename(landed_path)` — the publisher-side filename, UNCHANGED, no prefix (Д2).
3. Own: for each `linked_set_ids(conn, project_id)` set, the LIGHT frames (the reproduced `union_light_frames` SQL — see Consumes; do NOT import the render-gated original), each with a `get_light_calibration_for_frame` row whose `output_path` exists on disk (missing file ⇒ skip + `warn!`); metadata from the `frames` row; `file_path = output_path`, `filename` = its basename. These form their own dataset keyed by `own_display` (a received publisher with the identical display name would merge into it — fine, dedup rule 4 still applies per `frame_uuid`).
4. Dedup guard: a frame that is BOTH own and received (the processor also contributed and later downloaded its own package — normally impossible since own announcements aren't downloaded, but a coordinator review copy of its own... cannot happen either [own announces skip pending-to-self]; still) — dedupe by `frame_uuid` ACROSS all datasets: own wins, received duplicate skipped with `debug!`.
5. Within each publisher dataset, group into `ExportGroup`s by `(filter, CameraType::from_bayerpat)` via `ExportGroup::make_group_key`/`make_display_name`; **within each group create ONE subgroup PER `instrume`** (Д6 — `organize_subgroup` derives the camera folder from `subgroup.frames.first().instrume`, file_organizer.rs:203-209, so single-instrume subgroups give each of a publisher's cameras its own `camera_<instrume>/lights/`). Subgroups carry NO calibration nodes (`flat`/`dark`/`bias` = None — mirror the `CalibrationSubgroup { … }` construction at data_collector.rs:1028-1036, whose calib fields are None when links are empty; the `"Default"` display-name branch is `:1010-1014`). AUDIT field notes: `ExportFrame.frame_id`/`file_id` are REQUIRED i64 — use `-1` sentinels for received contributions (organizer never reads them; own frames use real ids); each `ExportData` also requires `object_name` (use the project's target name), `total_light_frames`, `total_exposure_seconds` (per-dataset counts); `calibration_summary` has NO Default — hand-construct it field-by-field (zeros/empties); `filters` = empty Vec, `master_plan` = Default.
6. Empty result (no contributions AND no own frames — `publishers` empty or all datasets frameless) ⇒ error "nothing to export for this project".

Runner rules: resolve `own_display` first via the MOVED helpers (Д2 — `own_display_name`/`member_node_ids` now in ungated `crate::collab::snapshot`; `own_node` via the `collab.rs:1210-1213` idiom, members parsed from `get_project().members_json`; unresolvable ⇒ `"own"` + `warn!`); register cancel flag under `-1` in `ctx.active_exports` exactly like the frame-set export (`ExportHandle { cancel_flag }`, insert/remove idiom `commands/export.rs:477-483`/`:596-599` — the registry is core-resident, `services/mod.rs:84`, so the runner reaches it directly); emit `"export-progress"` phase `"collecting"` → collect (spawn_blocking) → `let title_dir = Path::new(output_dir).join(sanitize_display_folder_name(&data.title))`, then **for each `(publisher, dataset)`** `organize_files_wbpp(&title_dir, &dataset, use_symlinks, &config, emitter.as_deref(), -1, &cancel_flag)`, summing `files_organized` and concatenating warnings; check the cancel flag between publishers (AUDIT: `load_wbpp_config` exists only as PRIVATE duplicates in both host crates (`commands/export.rs:79-95` / `routes/export.rs:47-63`, byte-identical) — reproduce it as a private helper in `api/collab_exchange.rs`; exact behavior: absent settings row ⇒ `Ok(WbppExportConfig::default())`, parse failure ⇒ `warn!` + `Err` — and call it with `.unwrap_or_default()` like both hosts do; do NOT refactor the host copies; note the emitter type seam: runner takes `Option<Arc<dyn ProgressEmitter>>`, organizer takes `Option<&dyn ProgressEmitter>` — `.as_deref()`. Accepted limitation, document on the runner: each organizer call emits percent against ITS OWN dataset total, so the progress bar restarts per publisher — the Task-2 dialog shows current file + publisher count, not one monotonic percent) → emit `"export-complete"` (`ExportCompleteEvent { frame_set_id: -1, success, files_organized (summed), warnings (concatenated), error, output_dir }`) → deregister; errors emit `export-complete` with `success:false` + the error string (never swallow).

- [ ] **Step 1: failing collector tests** (in `project_collector.rs`, in-memory conn + `init_db`): (a) two contributions from two publishers with the same basename land in TWO per-publisher datasets (`frame_set_name` = each display name), filenames = the original basename, unprefixed; (a2) one publisher with two filters ⇒ two `(filter, camera)` groups inside that publisher's single dataset; (b) a superseded contribution is excluded; (c) own calibrated frame lands in the `own_display` dataset with plain basename, and a linked-set frame WITHOUT a calibration row (or with a missing output file — use a tempdir path that doesn't exist) is skipped with a warning; (d) malformed frame_meta skips the row, others survive; (e) empty ⇒ error. Fixtures: real `project_packages`+`project_contributions` inserts (reuse the db::collab_exchange test idioms) + real frames/files/light_calibrations rows for the own side (mirror `api/collab.rs`'s publish-test fixture SQL).
- [ ] **Step 2:** implement collector + the Д2 helper move (`own_display_name`/`member_node_ids` from `api/collab.rs:1086`/`:1073` → `crate::collab::snapshot` as `pub(crate)`; update the `api/collab.rs` call sites to import them); focused tests green.
- [ ] **Step 3:** implement the runner + one api-level test: seed a project + one contribution with a REAL tiny FITS as the landed file (`fits_writer::write_fits_f32`), run `export_project_for_wbpp` into a tempdir with `use_symlinks:false`, assert the tree `<out>/<title>/<publisher display>/camera_<instrume>/lights/<original basename>.fits` exists byte-identical, `ExportResult.files_organized == 1`.
- [ ] **Step 4:** gates (`cargo test -p athenaeum-core --lib export`, full core, perseus headless build, workspace) + commit `feat(collab): project-scoped WBPP export — contributions ∪ own calibrated outputs`.

### Task 2: Export command (both backends) + ProjectDetail UI

**Files:**
- Modify: `crates/athenaeum-tauri/src/commands/collab.rs` + `lib.rs` `invoke_handler![]`; `crates/athenaeum-web/src/routes/collab.rs` + `routes/mod.rs`
- Create: `src/components/collab/ProjectExportDialog.tsx`; Modify: `src/pages/ProjectDetail.tsx` (button lives next to the Receive tab content; the tab body itself is `src/components/collab/ReceiveTab.tsx` — put the button wherever the layout reads best, both files are in scope)
- Modify: `docs/export/README.md` — the doc currently claims "exactly three files … Nothing else exists under `export/`" and "This is the entire feature"; Task 1's `project_collector.rs` + this command falsify both. Add a short "Project-scoped export (collab)" section: the Д2 tree shape, contributions ∪ own calibrated outputs, `-1` sentinel, and that per-publisher subtrees carry no calibration folders (calibration-off by design).

**Interfaces:**
- Command `export_collab_project` (Tauri) / `POST /api/export_collab_project` (web): args `{projectId: String, outputDir: String, useSymlinks: bool}` → `ExportResult` (already TS-exported — generated into `src/types/export.ts:435-459`, NOT models.ts). Tauri wrapper builds `TauriProgressEmitter`; web uses `SseProgressEmitter` AND validates `output_dir` starts with `state.export_dir` (mirror `routes/export.rs:629-636` EXACTLY, including the shape: it returns HTTP 200 with `make_fail(...)` — an `ExportResult { success:false, error: Some("Export path must be within …") }` body, NOT a 4xx — and is skipped entirely when `state.export_dir` is `None`). Cancel: reuse the EXISTING `cancel_export` command with `frame_set_id = -1` (document on the wrapper; frontend idiom `api.invoke('cancel_export', { frameSetId })`, `useExportProgress.ts:197`).
- UI: an "Export for WBPP" button on the **Receive tab** (processor payoff; visible for the same roles as the tab — `canReceive`, `ProjectDetail.tsx:156`) opening `ProjectExportDialog`: dir picker (Tauri → `pickDirectory()` from `src/api/desktop.ts:49-55`; web → `get_export_dir` auto-fill (`ExportTab.tsx:152-158`) + `FolderBrowserModal` for browsing (`ExportTab.tsx:256-265`); `isTauri` from `src/utils/platform.ts`), symlink toggle (Tauri non-Windows only, mirror the eligibility calc `ExportTab.tsx:299-305`), Export button → invoke. AUDIT correction: only the completion NOTIFICATION is free (`useExportProgress` fires `notify()` unconditionally on `export-complete`, tolerates -1); live progress is NOT — the hook's `activeExports` entries are created solely by its own `startExport` (hard-coded `export_to_wbpp`), so a project export never registers with the global indicator. Task 2 therefore shows DIALOG-LOCAL progress: a cancelled-flag `api.listen('export-progress')` filtered on `frameSetId === -1` driving a percent bar in `ProjectExportDialog`, plus a busy state from the invoke promise; a Cancel button invokes the existing `cancel_export` command with `frameSetId: -1`. Note (Д2): the percent restarts for each publisher subtree — show "publisher m/n" alongside the bar rather than pretending one monotonic total. Empty-state honesty: no pre-flight readiness duplication in the UI — the dialog always opens and a project with nothing exportable surfaces the backend's "nothing to export for this project" error inline (collector rule 6 is the single source of truth).

- [ ] **Step 1:** wrappers both backends (thin; instrument attrs; web export-dir validation) + registrations.
- [ ] **Step 2:** `ProjectExportDialog.tsx` + Receive-tab button (design tokens; inline `text-error` on failure; disabled while running) + the `docs/export/README.md` project-export section (fix the now-stale "exactly three files"/"entire feature" claims).
- [ ] **Step 3:** `npx tsc --noEmit` clean; full gates; commit `feat(collab): project WBPP export command + Receive-tab export dialog`.

### Task 3: Notifications polish (Д4)

**Files:**
- Modify: `crates/athenaeum-core/src/api/collab_exchange.rs` (poll diff: `awaitingApproval`; doc-comment kind list)
- Modify: `src/hooks/useSyncStatus.ts` (`notifyFinished` project-aware branches + nudge skip)
- Modify: `src/hooks/useProjects.ts` (map `awaitingApproval` + the Д4d joined-project diff)

**Interfaces:** `PackageStateChange.kind` gains `"awaitingApproval"` (kind is a plain `String` in Rust / `string` in TS — models.ts:898 — so NO type change, only the doc-comment union at `collab_exchange.rs:488` + mapping); documented kinds become `newPackage | approved | rejected | downloadFailed | awaitingApproval` (`downloadComplete` REMOVED — VERIFIED never emitted anywhere in the crate, and `useProjects.ts:94` already default-arms it; no test asserts the doc list). `SyncFinishedEvent` (TS: models.ts:716-748) carries `projectId: string | null`, `direction`, `outcome`, `okCount`, `failed`, `newCount`, `duplicateCount` — "sent+confirmed" means `direction === 'sent' && outcome === 'confirmed'` (there are no sent/confirmed count fields).

Behaviors:
1. Rust: in `apply_announcements`, the unknown-row arm (`collab_exchange.rs:548-556`) additionally emits `awaitingApproval` when `state == "pending" && !own` (hub visibility already restricts foreign pending rows to coordinators — no role check needed app-side; say so in a comment). Existing kinds unchanged.
2. `notifyFinished` (`useSyncStatus.ts:30-109`): at the TOP, branch on `p.projectId != null`: sent+confirmed → title "Contribution replicated", detail "N frames delivered — safe to go offline" (N = `okCount`), `kind:'project'`, `link:'/projects/'+projectId`, dedupe `collab-sent-<packageId>`; sent+failed → project-toned failure (kind 'project', warning; reuse the existing `failed:` reason-extraction idiom `:40-53`); received ok (`okCount > 0`) → "Project package downloaded" + okCount detail, dedupe `collab-recv-<packageId>`, link to the project; received failed → warning equivalent. The sync-incoming-unconfigured NUDGE (`:79-98`) must not fire for project events (early return before it). Personal-sync behavior byte-identical when projectId is null. Sent+cancelled stays silent (same as personal).
3. `useProjects`: map `awaitingApproval` → notify "Contribution awaiting your approval" (no detail — the change carries none), `kind:'project'`, tone 'info', dedupe `pkg-approval-<packageId>`, `link:'/projects/'+projectId` (mirror the existing kind arms `useProjects.ts:49-98`, titles via `titleFor`).
4. `useProjects` joined-project diff (Д4d): keep a `knownIdsRef: Set<string> | null` (null until a baseline loads); baseline = the cached `list_collab_projects` result on mount (`useProjects.ts:110`) and every successful refresh thereafter. In `refresh()` (`:20`), after `fresh: ProjectCard[]` arrives (`:24`): if `knownIdsRef.current !== null`, for each card with `!knownIds.has(projectId) && !card.coordinator` → notify title `Joined "<title>"`, detail 'Your join request was approved.', `kind:'project'`, tone 'success', `link:'/projects/'+projectId`, dedupe `project-joined-<projectId>`; then update the ref from `fresh` (always, even when the diff was skipped).

- [ ] **Step 1:** rust poll test: coordinator-view fixture — an unknown foreign PENDING announcement produces `awaitingApproval` (and NOT `newPackage`); a second poll produces nothing (row known). Update the kind-list doc comment (`collab_exchange.rs:488`) and regenerate models.ts (`TS_RS_WRITE=1 cargo test -p athenaeum-core --test ts_contract`) — never hand-edit; no existing test asserts the old kind list (verified).
- [ ] **Step 2:** frontend branches (behaviors 2-4); `npx tsc --noEmit`.
- [ ] **Step 3:** full gates; commit `feat(collab): notifications polish — replicated/downloaded/joined project toasts, awaiting-approval diff`.

### Task 4: Three-instance E2E (§11, Д5)

**Files:**
- Create: `crates/athenaeum-core/src/api/collab_e2e_tests.rs`; Modify: `crates/athenaeum-core/src/api/mod.rs` (`#[cfg(test)] mod collab_e2e_tests;` — test-only module, gated additionally by the render feature since it drives `publish_collab_frames`: use `#[cfg(all(test, feature = "render"))]`)

**Interfaces (all verified by the harness explorer — copy-ready):**
- Template: `download_happy_path_over_loopback` (`api/collab_exchange.rs:2016-2209`, attrs `#[cfg(unix)]` + multi-thread tokio) — three loopback endpoints, `ProjectReceiveHooks` (`receiver.rs:104`; fields `gate`, `announcements_refresher`, `on_project_ingested`, `request_handler`), `SyncRuntime::new()` (`receiver.rs:744`, pub) + `set_started_for_test(ep, handle, "ticket")`. Visibility VERIFIED from a sibling `api::collab_e2e_tests` module — **reachable crate-wide**: `store_token_for_test` (`account.rs:365-366`, `#[cfg(test)] pub(crate)`), `SyncRuntime::set_started_for_test` (`receiver.rs:781-782`, same), `LoopbackNetwork`/`LoopbackTransport` (`crate::sharing::loopback`, pub + ungated), `allow_all_peers` (`receiver.rs:61`), `StandaloneSyncStore` (`store.rs:571`), plus the pub fns of `collab_exchange` itself (`reconstruct_serve_dir` `:72`, `report_have_after_ingest` `:795`, `handle_project_request` `:343`). **NOT reachable (private in `collab_exchange::tests` — REPRODUCE minimal local copies):** `wire_hub` (`:1733`), `seed_project` (`:1121`), `seed_received_package` (`:1216`), `wait_until` (`:1545`), `own_node_for` (`:1743`), `hash_bytes` (`:1105`).
- Publish fixtures (verified): `api/collab.rs`'s test mod — `seed_publishable_set` (`:2347`, real 4×4 FITS via `write_fits_f32` `:2411` + files/frames/session_members/frame_analysis rows + `upsert_light_calibration`), `seed_publish_project` (`:2305`, `collab_projects` row with passed-in `members_json`), linked set via `link_frame_set(&ctx, project, set_id)` (`:2529` idiom). Closest scenario template: `pending_publish_seeds_the_coordinator` (`:2658`). These are private to `collab::tests` — mirror the fixture SQL locally (same reproduce-not-widen rule).
- `publish_collab_frames(ctx, collab_sender: &SyncSenderRuntime, project_id, emitter)` (`collab.rs:1127`, render-gated module — fine under this test's cfg), `refresh_project_packages(ctx, project_id)` (`collab_exchange.rs:649`), `decide_announcement(ctx, announcement_id, approve, reason)` (`collab.rs:1664`, render-gated), `download_project_package(ctx, sync: &SyncRuntime, project_id, package_id)` (`collab_exchange.rs:869`), `list_moderation_queue(ctx, project_id)` (`collab.rs:1589`, render-gated), `handle_project_request(ctx, sender, from: NodeId, project_id: String, package_id: String, emitter)` (`collab_exchange.rs:343`), Task-1's `export_project_for_wbpp`, `wait_until` idiom (`collab_exchange.rs:1545-1556`), `#[tokio::test(flavor = "multi_thread", worker_threads = 4)]` + `#[cfg(unix)]`.

Scenario (one test, `three_instance_project_flow_publish_moderate_deliver_export`):
1. Three `test_ctx`-style contexts (tempdir file DBs): CONTRIB, COORD, PROC. One `LoopbackNetwork`; endpoints: CONTRIB send, COORD recv+send (serves after approval), PROC recv+request. One wiremock hub; each ctx `wire_hub` + distinct `store_token_for_test` token.
2. Seed the project in all three caches (`require_approval: true`; members_json: COORD = coordinator send_receive with COORD's recv node, CONTRIB = send with its node, PROC = send_receive with its node — REAL node ids from the loopback endpoints, base64-encoded).
3. CONTRIB: seed 2 gate-passing frames (real FITS + analysis + calibration rows); wiremock `POST /projects/{P}/announcements` returns `{id: "ann-1", state: "pending"}`. `publish_collab_frames` → assert `state == "pending"`, `seed_target == Some(coordinator display)`, and the loopback engine was enqueued toward COORD's node.
4. COORD: its receiver (gate = members contains CONTRIB's node for P) ingests the push-seed — `wait_until` COORD's `project_packages.local_status == 'complete'` (COORD's db needs the package row first: wiremock `GET /projects/{P}/announcements` serves the pending announcement WITH `aggregateStats.manifestXxh3` = the anchor CONTRIB computed — read it from CONTRIB's `project_packages` row after publish (column `manifest_xxh3: Option<String>`, `PackageRow` `collab_exchange.rs:50`); mount AFTER publish). `list_moderation_queue` shows 1 item with 2 frames + metrics.
5. Approve: wiremock `POST /announcements/ann-1/approve` → `{id, state: "published"}`; `decide_announcement(approve)` → COORD local state `published`.
6. PROC: wiremock list now serves the announcement `published` with holders = [COORD's recv node pubkey b64] (remount the list mock — `mount_as_scoped` phases or a stateful Respond impl; keep it simple: unmount/mount sequence). COORD's receiver hooks include a `request_handler` wired like the template (spawns `handle_project_request` with COORD's ctx + a sender runtime). `download_project_package(PROC ctx, PROC sync runtime, P, pkg)` → `wait_until` PROC `local_status == 'complete'`; wiremock `POST /api/v1/announcements/ann-1/have` `.expect(1)` + an explicit received-requests recount, exactly like the template (`:2174-2179` + `:2199-2206`) — PROC reports once.
7. PROC: `export_project_for_wbpp` into a tempdir → assert the WBPP tree: `<out>/<project title>/<CONTRIB display>/camera_<instrume>/lights/` contains exactly 2 files carrying their original publisher-side names, byte-identical to CONTRIB's stamped payloads; PROC `files`/`frames` counts still 0 (catalog isolation, §11).
8. Teardown: shutdown engines/receivers.

- [ ] **Step 1:** build the fixture layer (contexts, hub mocks, members_json with real node ids) — compile + a smoke assertion (publish returns pending).
- [ ] **Step 2:** the full scenario through step 7; deflake with `wait_until` on SQL state only (no fixed sleeps).
- [ ] **Step 3:** full gates (the test runs in `cargo test -p athenaeum-core`; keep it under ~30s wall) + commit `test(collab): three-instance E2E — publish → moderation → swarm delivery → project WBPP export`.

## Security requirements (bind Tasks 1-3)

- S1: the web export command validates `output_dir` under `state.export_dir` exactly like the frame-set export (path escape = same rejection); the Tauri side takes the user-picked dir as-is (desktop trust model, matches existing export).
- S2: collector paths come ONLY from local db rows (`landed_path`, `light_calibrations.output_path`) — never from user/hub input; the organizer's skip-existing + warn-on-missing semantics are preserved (no overwrite primitive).
- S3: publisher display names become FOLDER names (Д2) and are sanitized by the organizer itself (`sanitize_display_folder_name` on `frame_set_name`) — no separator/traversal characters survive; the collector must NOT pre-join display names into paths itself.
- S4: notification strings render as React text (no markup); project ids in links are route params, not interpolated HTML.
- S5: UI honesty — export success/failure from `ExportCompleteEvent` only; dialog errors inline; no optimistic states.

## Post-plan checklist (verification/ops notes, not tasks)

- The slice-4 live smoke is still OWED (two instances vs test-hub: publish → download → moderation) — slice 5's E2E covers the same flow in-process, but the live smoke exercises real iroh + the real hub; run it after this slice (plus watch observatory-Perseus personal sync after the connect gate).
- Hub follow-up (recorded, out of app scope): no un-have on the wire — stale have-reports for superseded packages persist hub-side.
- Deferred follow-ups from slice 4's final review carry unchanged (M1 connect-gate install-on-sign-in, M3 created_at format mixing, M4 spawn_blocking+tmp-rename for serve copies, dialog a11y sweep, formatBytes/http_util consolidation).

## Self-review notes (applied while writing; re-verified against code 2026-07-14 by a three-agent audit)

- The export module is verified UNGATED and `RawWithCalibrationSets` is a no-op transform — the project export path needs no render code; only the E2E (which drives publish) is render-gated, expressed as `#[cfg(all(test, feature = "render"))]`. Perseus depends on core with `default-features = false` (`perseus/Cargo.toml:30`) — code added to `api/collab_exchange.rs` compiles headless as long as it touches only ungated deps.
- **Gating trap found by the audit:** `own_display_name`/`member_node_ids` and `union_light_frames` live in render-gated `api::collab` and are private — the ungated collector/runner must MOVE the display helpers to `crate::collab::snapshot` and REPRODUCE the union SQL (Task 1). Similarly `decide_announcement`/`list_moderation_queue` are in render-gated `api/collab.rs` (`:1664`/`:1589`), not `collab_exchange` — fine for the render-gated E2E, fatal if ever referenced from the runner.
- `frame_meta` keys are snake_case (`models::Frame` has no serde rename) — the collector parses `filter`/`bayerpat`/`instrume`/`exptime` etc. by those names; `filename`/`file_path` do NOT exist in frame_meta and come from `landed_path` (received) / `output_path` (own).
- `organize_files_wbpp` ignores `keyword_order` today (hard-coded nesting) — the project export inherits that; no new config surface.
- The `-1` sentinel cannot collide with real frame-set ids (AUTOINCREMENT positive); `cancel_export(-1)` rides the existing command (pure `active_exports` HashMap lookup, no DB — verified both hosts). The new runner builds `ExportData` directly and never calls `collect_export_data`, so the `get_frame_set_info` DB lookup that would abort on `-1` is never on the path.
- `useExportProgress` verified: the `export-complete` notify fires unconditionally (`useExportProgress.ts:82-92`, no name lookup) — a `-1` completion produces a sane `kind:'export'` notification for free; only live progress needs the Task-2 dialog-local listener.
- `downloadComplete` verified never emitted anywhere in the crate (doc-comment-only at `collab_exchange.rs:488`); the frontend already default-arms it — removal is doc-only, zero behavior change.
- Visibility of test seams from the new E2E module is verified item-by-item in Task 4 (reachable vs reproduce lists).
- §9 spec coverage swept: replicated/downloaded/awaiting-approval (Д4a-c), approved/rejected/newPackage (shipped slice 4), "frame set matches project X" (shipped slice 3, `useProjectMatches.ts`), "join request approved" (Д4d — was the one uncovered item).
